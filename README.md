# cuberactcom.github.io

This repo serves **www.cuberact.com**, which automatically redirects to **www.cuberact.org**.

The redirect is handled by a `<meta http-equiv="refresh">` tag in `index.html` with zero delay — the browser navigates to cuberact.org immediately, without any visible page load.
